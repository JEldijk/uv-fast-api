# The main pipeline for python
resources:
  pipelines:
    python_pipeline:
      name: python_pipeline
      target: python_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: /Workspace/${workspace.file_path}/src
